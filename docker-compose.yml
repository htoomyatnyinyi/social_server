# Docker Compose Modular Services
# Run: docker compose up -d (starts both sub-projects)

include:
  - path: social-app/docker-compose.yml
  - path: zipline/docker-compose.yml

networks:
  social-network:
    name: social-network
    driver: bridge
